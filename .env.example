# Environment Variables for FHEVM Starter Hardhat Project

# Private key for deployment (without 0x prefix)
# For local development, you can leave this empty to use hardhat's default accounts
# For testnet/mainnet deployment, set your actual private key
PRIVATE_KEY=

# RPC URL for the network you want to deploy to
# Local development (default)
RPC_URL=http://127.0.0.1:8545
# For FHEVM testnet, replace with actual testnet RPC URL

# Contract address after deployment (set this after deploying)
COUNTER_ADDR=

# Optional: Network name (dev, testnet, mainnet)
# NETWORK=dev